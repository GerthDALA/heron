# URL of the FastAPI backend, as reached from the browser
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# URL of the backend as reached from the Next.js server (SSR + proxy)
API_INTERNAL_URL=http://localhost:8000/api/v1
